# SQL Server connectivity -- one of these per platform, picked automatically
# by `pip install` via the environment markers below (see db.py for why
# there are two backends).
pyodbc>=5.0; sys_platform == "win32"
python-tds>=1.13; sys_platform != "win32"

# Modern ttk theme (Windows 11-style, light/dark), applied in
# name_selector.py's _apply_theme_styles(). Pure Python + tkinter, no
# platform-specific concerns.
sv-ttk>=2.6.0

# Packaging (only needed to build a standalone .exe/.app -- not needed to
# just run the app with `python name_selector.py`)
pyinstaller>=6.0

# Everything else is standard library:
#   tkinter, ttk, messagebox, simpledialog, collections, os, time,
#   datetime, calendar, threading, configparser, sys, re
#
# tkinter ships with the standard CPython installer on both Windows and
# macOS, so nothing beyond the above needs to be pip-installed.
#
# On Windows, pyodbc also needs a matching ODBC driver for SQL Server
# installed on the machine (a Windows component, not a pip package) --
# see README.md for how to check what's already installed.
#
# On macOS, python-tds needs nothing else installed -- that's the whole
# point of using it there (see db.py's module docstring).

# Powers optional NTLM domain-login authentication on macOS/Linux (see
# README.md's "Domain authentication on macOS" section) -- lets users
# authenticate with their own domain username/password instead of a SQL
# Server login, with no system driver and no Homebrew. Pure Python + the
# `cryptography` package (prebuilt wheels, no compiler needed), so it's
# just always available; the connection dialog offers it as a choice and
# never requires it.
pyspnego>=0.10.0; sys_platform != "win32"
